# API Configuration
VITE_API_BASE_URL=https://rnzor.github.io/awesome-tech-failures/api/v1

# Optional: OpenAI API Key for custom embeddings (NOT recommended for production)
# Leave empty to use hybrid lookup only (pre-embedded terms)
# VITE_OPENAI_API_KEY=

# Development mode (overrides API base URL)
# VITE_DEV_MODE=false
